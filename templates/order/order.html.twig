{% extends 'base.html.twig' %}

{% block title %}Ma commande{% endblock %}

{% block body %}
<main class="d-lg-flex justify-content-between">
  <div class="valid-card">
    {{ form(form) }}
  </div>
  <div class="card valid-card">
    <div class="card-body">
    {% for item in order.items %}
    <p class='m-0'>Nom du produit : {{ item.product.title }} </p>
    <p>Quantité : {{ item.quantity }}</p>
    {% endfor %}
    </div>
  </div>
</main>
{% include 'partials/infos.html.twig' %}
{% endblock %}